"use strict"

// STRUCTURE
module.exports = {
  midelware: require('./data/midelware'),
  model: require('./data/model'),
  controllers: require('./data/controller'),
  valide: require('./data/validatorSchemas'),
  scheme: require('./data/schemas/schema')
}
